# python
__pycache__/
*.py[cod]
.venv/
.pytest_cache/

# environment & secrets
.env

# OSMnx HTTP response cache (large, regenerable)
data/osmnx_cache/

# NOTE: data/cache/ is intentionally tracked -- it holds the pinned
# precomputed demo results needed for offline presentations.

# editor/os noise
.vscode/
.idea/
Thumbs.db
.DS_Store

# claude code local state
.claude/settings.local.json

# cloudflared binary + tunnel log (besar, lokal)
tools/

# node / capacitor (android/ shell IS tracked; deps are not)
node_modules/
android/.gradle/
android/app/build/
android/build/
android/local.properties
android/.idea/
android/app/src/main/assets/public/
